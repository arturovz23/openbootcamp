//Notaciones

//; --> delimita el final de una linea (recomendado como buena practica)

//. --> este se utiliza en los objetos para acceder a los atributos
//movil.toString

//[] --> se utilizan para las listas, arreglos o arrays
const ar = [1,2,3,4];
console.log(ar[2]);

//() --> se utilizan para las funciones
function nombre(e) {
    //Se escribe la funcion
}

//{} --> Llaves para los objetos, funciones y estructuras de control
const movil = {
    anchura:5,
    altura: 10,
}

if (true) {
    //corre todo lo que este entre las llaves
}